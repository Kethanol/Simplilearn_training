using System;
using System.IO;

namespace RetrieveStudentFromFile.Constants
{
    internal struct File
    {
        public const string PATH = "\\assets\\student_data.txt";
        public const string ERROR = "The item assets/student_data.txt inside the active directory is unavailable, unaccessible or the path is incorrect!";
    }
}
